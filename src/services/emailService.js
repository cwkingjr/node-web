'use strict';

// Remove this once 'user' param is referenced
/* eslint no-unused-vars: "off" */

// TODO complete stub out
function sendUserVerificationEmail(user) {
	return Promise.resolve(true);
}

module.exports = {
	sendUserVerificationEmail
};